import 'package:flutter/material.dart';

class QBottomSheet extends StatelessWidget {
  const QBottomSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
