import 'package:flutter/material.dart';

class QTextButton extends StatelessWidget {
  const QTextButton({super.key});

  @override
  Widget build(BuildContext context) {
    return TextButton(
      onPressed: () {},
      child: const Text('ok'),
    );
  }
}

class QIconButton extends StatelessWidget {
  const QIconButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class QSvgButton extends StatelessWidget {
  const QSvgButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

/// Shadow
/// Radius
/// padding
/// border color,
